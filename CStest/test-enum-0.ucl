module main {
  var systemcall : enum { setresuid, setfsuid, noop };
  var a: integer;
  init {
    systemcall = noop;
    a = 0;
  }
  
  next {

  }
  
  control {
    //v = bmc(5);
    concrete(5);
    check;
    //print_results;
  }
}
