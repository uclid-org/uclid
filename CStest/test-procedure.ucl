module main
{
  type num_t = integer;
  var numbers : [boolean, num_t]integer;
  var zero : integer;
  var read: integer;
  var a: integer;

  procedure f()
    modifies numbers;
    modifies a;
    {
        numbers[true, 0] = numbers[true, 0] + 1;
        a = a + 1;
    }

  init {
    numbers[true, 0] = 1;
    a = 0;
    read = numbers[true,0];
  }

  property check_0: (numbers[true,0]!=2);

  next {
    call f();
  }
  
  control {
    v = concrete (5);
    check;
    v.print_concrete_trace(a,numbers);
    //print_results;
    //v.print_cex(numbers);
  }
}
