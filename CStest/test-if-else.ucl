module main
{
    var a : integer;
    init {
        a = 0;
        if (a < 1) {
            a = 1;
        } 
    }
    next {
        if (a < 1) {
            a' = 0;
        } else {
            a' = 2;
        }
    }
    property jump_a: a!=3;

    control {
        v = bmc(5);
        check;
        print_results;
        //v.print_cex(n);
    }
}
