module main
{
    var a : integer;
    init {
        a = 0;
    }
    next {
        a' = a + 1;
    //    assert(a !=3);
    }
    property jump_a: a!=3;

    control {
        v = concrete(5);
        check;
        v.print_concrete_trace(a);
    }
}
