//we gather 2 Syntax Error
//Error Type is More words
//we have Syntax Error on line10
//we have Syntax Error on line9
//we have Syntax Error on line20
module main
{
  type num_t = integer;
  var numbers : [{boolean, num_t}]integer;
  var p1, p2  :  {boolean, num_t};

  init {
    p1 = {true, 10};
    assert (p1 == {true, 10});
	assume(p1== unroll p2);
	} (p1 
  next {
    assert (numbers[p1] != numbers[p2]);
  }

  control {
    unroll (1);
    check;
    print_results;
  }
	} 10}; 
