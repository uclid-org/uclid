//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-modules-4.ucl' mode='r' encoding='UTF-8'>
//We have error on line 28
//We have error on line 22
module incr
{
  input x : integer;
  output y : integer;
  var c : integer;
  
  init {
    c = 0;
  }
  
  next {
    c' = c + 1;
    y' = x + c';
  }
}

module main
{
  var a, b : integer;
  
  instance incr1 : incr(x : (a), y: (b));
  
	init{ 
    a = 0;
    b = 0;
  }
  procedure do_next()
    modifies a;
	{ integer; 
    a' = a + 1;
    next(incr1);
  }
  next {
    a' = a + 1;
    next (incr1);
  }
  
  invariant b_prop: (b >= 0 && b >= a);
}
