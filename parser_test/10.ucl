//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-cex-json-record.ucl' mode='r' encoding='UTF-8'>
//We have error on line 3
//We have error on line 24
module cpu {
    type obs_t = record {
        cache       : [bv4]bv4
	

    var cache       : [bv4]bv4;
    var obs         : obs_t;

    next {
        havoc obs;
    }
}

module main {
    instance cpu1 : cpu();

    next {
        next(cpu1);
    }

    invariant trivial : (forall (a : bv4) :: cpu1.obs.cache[a] == 0bv4);

    control {
        v = bmc(2);
	
        print_results;
        v.print_cex_json(cpu1.obs);
    }
}
