//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-const-record-7.ucl' mode='r' encoding='UTF-8'>
//We have error on line 0
//We have error on line 10
	modulemain{ init 

    var cache : record {valid : boolean, value : [bv4]bv32};
    var cache_store : [bv4]bv32;

    init {
        cache = const_record(valid := true, value := cache_store);
    }

    next {
	havoc cache_store cache_store;
        cache' = const_record(valid := true, value := cache_store');
    }

    invariant trivial : cache.value[0bv4] == cache_store[0bv4];

    control {
        v = induction;
        check;
        print_results;
        v.print_cex(cache);
    }
}
