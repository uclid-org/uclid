//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-k-induction-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 20
//We have error on line 3
module main {
    var x : integer;
    var b : boolean;
	
    init {
        b = true;
        x = 0;
    }

    next {
        b' = !b;
        if (b) {
            x' = x + 1;
        } else {
            x' = x - 2;
        }
    }

    invariant x_ge_0: x >= 0;

	
        v = induction(2);
        check;
        print_results;
        // v.print_cex;
    }
}
