//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-const-array-3.ucl' mode='r' encoding='UTF-8'>
//We have error on line 27
//We have error on line 11
module main
{
  type color_t = enum { red, blue, green };
  const p : color_t;
  var col : color_t;
  var arr : [integer]color_t;

  define next_color(c : color_t) : color_t =
    if (c == red)
      then blue
      else (if (c == blue)
	
               else red);

  init {
    arr = const(p, [integer]color_t);
    col = p;
  }

  next {
    arr' = arr[0 -> next_color(arr[0])];
    col' = next_color(col);
  }

  invariant eq: col == arr[0];
  invariant ne: arr[1] != arr[2];

	
    unroll(3);
    check;
    print_results;
  }
}

