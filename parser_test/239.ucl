//we gather 3 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/parser_error_2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 4
//We have error on line 2
//We have error on line 1
//We have error on line 9
module main
	
	

	
        assume (0 < x);
        assume (x < y);
        assume (y < z);
    }
	
    next {
        x' = x + y;
        y' = x' + 1;
        z' = z + y';
    }

    invariant x_gt_0: x > 0;
    invariant y_gt_0: y > 0;
    invariant z_gt_0: z > 0;

    control {
        v = induction[pre=[y_gt_0]; properties=[x_gt_0]];
        check;
        print_results;
    }
}