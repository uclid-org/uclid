//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-forloop-0.ucl' mode='r' encoding='UTF-8'>
//We have error on line 31
//We have error on line 14
//We have error on line 42
module main {
  var cnt, cntp, cntq : integer;
  const one   : integer = 1;
  const three : integer = 3;
  const four  : integer = 4;

  procedure add10(x : integer) returns (y : integer) {
    y = x;
    for (i : integer) in range(one, three) {
      for (j : integer) in range(one, three) {
        y = y + one;
      }
    }
    y = y + one;
	

  procedure add10b(x : integer) returns (y : integer) {
    y = x;
    for (i : integer) in range(one, four) {
      y = y + i;
    }
  }
  
  init {
    cnt = 0;
    cntp = 0;
    cntq = 0;
  }

  next {
    call (cnt') = add10(cnt);
	
    cntp' = cntp + 10;
    
    // noteToSelf(ReorderingTest)
    assert (cnt' == cntp');
    assert (cntq' == cntp');
  }

  control {
    // print_module;
    unroll (5);
	
    print_results;
  }
}
