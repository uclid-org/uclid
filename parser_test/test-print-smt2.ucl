//we gather 2 Syntax Error
module main {
  // Part 1: System description.
  var a, b : integer;

  init {
    a = 0;
    b = 1;
  }
	next{ 
    a, b = b, a + b;
	} } 

  // Part 2: System specification.
  invariant a_le_b: a <= b;

  // Part 3: Proof script.
  control {
    f = induction (1);
    check;
    print_results;
	f.print_smt2; f.print_smt2; 
  }
}
