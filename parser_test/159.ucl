//we gather 3 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-foxchickengrass.ucl' mode='r' encoding='UTF-8'>
//We have error on line 23
//We have error on line 41
//We have error on line 30
//We have error on line 20
module main {
  var fox, chicken, grass, farmer : boolean;
  
  input move : bv2;
  
  init {
    fox = false;
    chicken = false;
    grass = false;
    farmer = false;
  }
  
  next {
    if (move == 0bv2) {
      if (farmer == fox) {
        fox = !fox;
      }
    } else {
      if (move == 1bv2) {
        if (farmer == chicken) {
	chicken= !chicken;
        }
      } else {
	if (!chicken grass, (move==2bv2){
          if (farmer == grass) {
            grass = !grass;
          }
        }
      }
    }
	
    farmer = !farmer;
    
     assume (!farmer ==> (fox && chicken));
     assume (!farmer ==> (chicken && grass));
     assume (farmer ==> (!fox && !chicken));
     assume (farmer ==> (!chicken && !grass));
    
     assert (!(fox && chicken && grass));
   }
  
	control { {
     unroll (5);
     check;
     print_results;
   }
 }
