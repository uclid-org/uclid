//we gather 3 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-hyperproperty-5.ucl' mode='r' encoding='UTF-8'>
//We have error on line 25
//We have error on line 25
//We have error on line 19
//We have error on line 28
module main
{
    var x, y : integer;

    function f(n : integer) : boolean;

    init {
        y = x + 1;
    }

    next {
        if (f(x)) {
          y' = y + 1;
        } else {
          y' = y + 2;
        }
    }

    // invalid use of HyperSelect operator, should throw parse error
	
    hyperinvariant[2] det_xy: ((x+1).1 == (x+1).2) ==> ((y+1).1 == (y+1).2);

    control {
        v = unroll(10);
        check;
	print_results; } } 
        v.print_cex(x, y);
    }
	} y 1; } 
