//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/old-test-float.ucl' mode='r' encoding='UTF-8'>
//We have error on line 30
//We have error on line 19
//We have error on line 28
module main
{
  var x : float;
  var y : float;
  var z : float;
  var w : float;
  var q : float;

  init {
    x = 0.0;
    y = 0.0; // test assign
    assume(z==w); // test assume

  }

  next {
    x' = y + 1.0;
    y' = y - 1.0;
    assume(q > 0.0);
	q'=2.0*q; { y 
    assert(isNaN(q));
    assert(q' < q); // should fail
  }

  invariant myinv:  x!=y; // should fail induction
  invariant inv2: z > w; // should fail
  invariant inv3: w < z;  // should fail

	
  control {
	f=induction; float; var 
    check;
    print_results;
  }
}
