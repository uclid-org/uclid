//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/induction-pre-control-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 10
//We have error on line 0
//We have error on line 11
	
{
    var x, y, z : integer;

    init {
        assume (0 < x);
        assume (x < y);
        assume (y < z);
    }

	
	
        y' = x' + 1;
        z' = z + y';
    }

    invariant x_gt_0: x > 0;
    invariant y_gt_0: y > 0;
    invariant z_gt_0: z > 0;

    control {
        v = induction[properties=[x_gt_0]];
        check;
        print_results;
    }
}

