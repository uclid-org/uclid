//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-array-new.ucl' mode='r' encoding='UTF-8'>
//We have error on line 33
//We have error on line 26
//We have error on line 23
module main
{
  var x, y, z : integer;

  init {
    x = 0;
  }

  next {
    if (x < 500) {
      var xp : integer;
      xp = x + 2;
      if (*) {
        x' = xp;
        y' = z;
        z' = y;
      } else {
        x' = xp;
        y' = y;
        z' = y;
      }
      assert (x' - 1 == x);
      assert (x + 2 == x');
	
  }

	

  control {
    v = unroll(5);
    check;
    print_results;
  }
	
