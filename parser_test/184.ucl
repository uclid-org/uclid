//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-history-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 9
//We have error on line 0
	

	var cpt,oldcpt : integer ;
	
	init {
		cpt=0;
	}
  
	next {
	
		oldcpt'=cpt;
	}

// should pass
	invariant pass0 : (cpt>1) ==> (history(cpt,1)==oldcpt) ;
// should fail
	invariant fail0 : (cpt<=1) ==> (history(cpt,1)==oldcpt) ;


	control {
		vobj = induction(2);
		check;
		print_results;
 		vobj.print_cex(cpt,oldcpt);
	}
	
}

