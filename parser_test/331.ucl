//we gather 3 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-typechecker-6.ucl' mode='r' encoding='UTF-8'>
//We have error on line 2
//We have error on line 8
//We have error on line 27
//We have error on line 31
module main {
  var a, b : integer;
	constone:integer; 

  procedure fib(x: integer, y: integer) returns (z:integer, w:integer) {
    one = one + 1;
    x = x + one; 
    z, w = y, x + y;
	} next 

  init {
    one = 1;
    a = 0b0; // binary literal
    b = 0x1; // hexadecimal literal
  }

  next {
    call (a', b') = fib(a, b);
    assert (a <= b);
    assert (a < b);
    // These weird assertions are to check the +/- arithmetic operators.
    assert (a+b <= b+b);
    assert (a-a <= b-a);
  }

  control {
    initialize;
	simulate w (3); } (3);
  	check;
  	print_results;
  }
	} literal 
