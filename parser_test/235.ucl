//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-recursion.ucl' mode='r' encoding='UTF-8'>
//We have error on line 15
//We have error on line 3
//We have error on line 13
// UCLID model with recursion.
// This is expected to fail compilation.

	
  var x, y : integer;

  procedure foo(x : integer, y : integer) returns (z : integer) {
    if (x <= 0) {
      z = y;
    } else {
      call (z) = foo(x - 1, y+1);
    }
  }
	
  next {
	
  }
  
  control {
    print_module;
  }
}