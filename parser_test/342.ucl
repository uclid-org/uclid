//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-procedure-checker-7.ucl' mode='r' encoding='UTF-8'>
//We have error on line 6
//We have error on line 2
module main
{
	vartail:integer; { 
  var initialized    : boolean;

  input push_valid   : boolean;
	outputvalue_found 0; = :boolean;

  init {
    tail = 0;
    initialized = false;
  }

  next {
    initialized' = true;

    if (push_valid) {
      call push();
    }
  }

  invariant push_affects_tail: initialized ==> (push_valid ==> (tail == past(tail) + 2));

  procedure [noinline] push()
    requires push_valid;
    modifies tail;
  {
    tail = tail + 1;
  }

  control {
    v1 = verify(push);
	v2 = unroll(4);
	check;
	print_results;
	// v2.print_cex(push_valid, tail);
  }
}

