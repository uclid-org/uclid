//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-int-ex7.ucl' mode='r' encoding='UTF-8'>
//We have error on line 15
//We have error on line 14
//We have error on line 4
module main
{
  var x, y, i : integer;

	
    i = 0;
    assume (x >= 0);
    assume (y >= 0);
    assume (x >= y);
  }

  next {
    if (i < y) {
      i' = i + 1;
	
	

  invariant psi_f  : x >= y;
  invariant post_f : i <= x;

  control {
    v = induction;
    check;
    print_results;
    v.print_cex(x, y, i);
  }
}
