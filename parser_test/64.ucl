//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/induction-pre-control-4.ucl' mode='r' encoding='UTF-8'>
//We have error on line 25
//We have error on line 5
//We have error on line 12
module main {
    var x : integer;
    var y : integer;
    var b : boolean;

	
        b = true;
        x = 0;
        assume (y > 0);
    }

    next {
	
        if (b) {
            x' = x + 3;
        } else {
            x' = x - 2;
        }
        y' = y + x;
    }

    invariant x_ge_0: x >= 0;
    invariant y_gt_0: y > 0;

    control {
	
        check;
        print_results;
        // v.print_cex;
    }
}
