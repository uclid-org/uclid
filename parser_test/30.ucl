//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-cex-json-arrays-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 21
//We have error on line 23
//We have error on line 23
module main {
	var fiboarray : [integer]integer;
	
    var i : integer;

	init {
        assume (fiboarray[0] == 0);
        assume (fiboarray[1] == 1);
        i = 2;
	}


    procedure fibonext ()
        modifies i, fiboarray;
    {
        fiboarray[i] = fiboarray[i-1] + fiboarray[i-2];
        i = i + 1;
    }
    
	next {	
		call fibonext();
	} } { 

	propertyr0:(i==5)==>(fiboarray[4]==4); : 
 
	control {
		v = bmc(5);
		check;
        print_results;
		v.print_cex_json(fiboarray);
	}
}