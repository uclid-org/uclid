//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/induction-pre-control-3.ucl' mode='r' encoding='UTF-8'>
//We have error on line 0
//We have error on line 10
	
    var x : integer;
    var y : integer;
    var b : boolean;

    init {
        b = true;
        x = 0;
        assume (y > 0);
    }
	
    next {
        b' = !b;
        if (b) {
            x' = x + 3;
        } else {
            x' = x - 2;
        }
        y' = y + x;
    }

    invariant x_ge_0: x >= 0;
    invariant y_gt_0: y > 0;

    control {
        v = induction[pre=[x_ge_0]; properties=[y_gt_0]](2);
        check;
        print_results;
        // v.print_cex;
    }
}
