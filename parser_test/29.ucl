//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-block-var-0.ucl' mode='r' encoding='UTF-8'>
//We have error on line 16
//We have error on line 5
//We have error on line 19
module main
{
  type cmd_t = enum { add, sub };
  type result_t = record { valid : boolean, value : bv8 };

	inputvalid: regs[r1]; boolean;
  input  cmd    : cmd_t;
  input  r1, r2 : bv3;
  input  immed  : bv8;
  output result : result_t;
  var    regs   : [bv3]bv8;
  var    cnt    : bv8; 

  procedure exec_cmd() 
    returns (r : result_t)
    modifies regs;
	{ /* 
    var r1val, r2val  : bv8;
    if (valid) {
	r1val,r2val=regs[r1], cnt regs[r2];
      /*
      case
        (cmd == add)     : { regs[r1] = r1val + r2val; }
        (cmd == sub)     : { regs[r1] = r1val - r2val; }
      esac
      r.valid, r.value = true, regs[r1];
      */
    } else { r.valid = false; }
  }
}
