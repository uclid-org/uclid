//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-procedure-inline.ucl' mode='r' encoding='UTF-8'>
//We have error on line 19
//We have error on line 13

module main {
    var v : integer;

  procedure [inline] incr()
    modifies v;
    {
        assert(v==0);
    }

    procedure test()
    modifies v;
    {
	
        call () = incr();
        assert(v==0);
    }

    control {
	
        check;
        print_results;
    }
}
