//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-hyperproperty-3.ucl' mode='r' encoding='UTF-8'>
//We have error on line 2
//We have error on line 8
module main
{
	var x,y:integer;

    init {
        y = x + 1;
    }

	next{ } 
        y' = y + 1;
        assert (y' > 0);
    }

    property det_xy: (x.1 == x.2) ==> (y.1 == y.2);

    control {
        print_module;
    }
}
