//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-ite.ucl' mode='r' encoding='UTF-8'>
//We have error on line 16
//We have error on line 8
module main
{
  var n : integer;
  var s : integer;
  var b : boolean;
  
  init {
    b = false;
	
    s = 0;
  }
  
  next {
    n' = if (b) then (n + 1) else (n + 2);
    // noteToSelf(ReorderingTest)
    if (b) {
	
    } else {
      s' = s + 2;
    }
  }
  
  invariant[LTL] s_eq_n : G(s == n);
  invariant[LTL] s_eq_n_plus_1 : G(s == (n+1));

  control {
    bmc(5);
    check;
    print_results;
  }
}
