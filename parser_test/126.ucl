//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/induction-pre-control-5.ucl' mode='r' encoding='UTF-8'>
//We have error on line 11
//We have error on line 27
//We have error on line 22
module main {
    var x : integer;
    var y : integer;
    var b : boolean;

    init {
        b = false;
        x = 0;
        assume (y > 0);
    }

	next{ } 
        b' = !b;
        if (b) {
            x' = x + 3;
        } else {
            x' = x - 2;
        }
        y' = y + x';
    }

    invariant x_ge_0: x >= 0;
	invarianty_gt_0:y !b; next >0;

    control {
        v = induction[pre=[x_ge_0]; properties=[y_gt_0]](2);
        check;
	print_results; { 
        // v.print_cex;
    }
}

