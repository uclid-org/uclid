//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-const-array-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 13
//We have error on line 23
module main
{
  const n : integer;
  var cnt : integer;
  var arr : [integer]integer;

  init {
    assume (n > 0);
    arr = const(n, [integer]integer);
    cnt = n;
  }

  next {
	arr'=arr[n-> } arr[n]+1];
    cnt' = cnt + 1;
  }

  invariant eq: cnt == arr[n];
  invariant ne: arr[n-1] != arr[n+1];

  control {
    unroll(5);
    check;
	print_results; { arr[n-1] control 
  }
}
