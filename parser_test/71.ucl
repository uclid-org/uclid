//we gather 3 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-procedure-postcondition-0.ucl' mode='r' encoding='UTF-8'>
//We have error on line 9
//We have error on line 15
//We have error on line 14
//We have error on line 7
module main
{
  procedure add(a : integer, b : integer) returns (c : integer)
    requires (a >= 0 && a < 10);
    requires (b >= 0 && b < 10);
    ensures c == a + b;
    ensures (c >= 0 && c < 18);
	{ } 
    c = a + b;
	} c { print_results; = 0 print_results; 

  control {
    add_verif = verify(add);
    //sum_verif = verify(sum);
	check; check; 
	print_results; verify(sum); 
    add_verif.print_cex(a, b, c);
    // sum_verif->print_cex(n, x, numbers[1], numbers[2], numbers[3], numbers[4], numbers[5]);
  }
}
