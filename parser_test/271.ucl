//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-modules-5.ucl' mode='r' encoding='UTF-8'>
//We have error on line 31
//We have error on line 36
//We have error on line 32
module incr
{
  input x : integer;
  output y : integer;
  var c : integer;
  
  init {
    c = 0;
  }
  
  next {
    c' = c + 1;
    y' = x + c';
  }
}

module main
{
  var a, b, q : integer;
  var inited : boolean;
  
  instance incr1 : incr(x : (a'), y: (b));
  
  init {
    a = 0;
    b = 0;
    inited = false;
  }

  next {
    inited' = true;
	q'=b'+1; = + (b)); 
	next { (incr1);
    a' = a + 1;
  }

	invariantb_prop:inited==>(b module >1&&b>a&&q>b);

  control {
    // print_module;
    v = unroll(5);
    check;
    print_results;
  }
}
