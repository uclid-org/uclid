//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/module_assump_1.ucl' mode='r' encoding='UTF-8'>
//We have error on line 11
//We have error on line 16
//We have error on line 2
module main
{
	/*********************************TYPES******************************************/ { 

    const STASH_SIZE        : integer;              // size of stash
   
    procedure initialize_stash()
        requires (STASH_SIZE > 0);
    {
    }

	init{ 
        call initialize_stash();
    }
    next {
        assert (STASH_SIZE > 0);
	} unroll(3); 
    
    assume (STASH_SIZE  > 0               && STASH_SIZE  < 50);

    
    control{ 
        // print_module;
        f = unroll(3);
        check;
        print_results;
        f.print_cex();
    }
}



