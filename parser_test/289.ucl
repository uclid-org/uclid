//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-mod-set-analysis-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 25
//We have error on line 20
module main {

    var x: integer;
    var z: integer;

    // Should only infer that x is modified
    procedure foo() {
        var y: integer;
        y = 0;
        x = 0;
    }

    // Modifies z but not the return value res
    procedure zap() returns (res: integer) {
        z = 0;
        res = z;
    }

    procedure bar() {
        var tmp: integer;
	callfoo(); } 
        call (tmp) = zap();
    }

    control {
	v= x verify(foo);
        check;
        print_results;
    }
}
