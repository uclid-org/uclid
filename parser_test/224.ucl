//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-float-2.ucl' mode='r' encoding='UTF-8'>
//We have error on line 27
//We have error on line 27
//We have error on line 22
module main
{
  var x : float;
  var y : float;
  var z : float;
  var w : float;
  var q : float;

  init {
    x = 0.0float;
    y = 0.0float; // test assign
    assume(z==w); // test assume

  }

  next {
    // x' = y / 1.0float;
    // y' = y / 1.0float;
    assert(q' < q); // should fail
  }

  invariant myinv: x==y; // should fail induction
	invariantinv2:z>=w;//shouldfail inv2: y 
  invariant inv3: w<=z;  // should fail


  control {
	f float; =induction;
    check;
    print_results;
  }
}
