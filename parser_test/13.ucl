//we gather 1 Syntax Error
//Error Type is More words
//we have Syntax Error on line18
//we have Syntax Error on line16
//this tests on the two keyword
module main
{
    var integer;

    init {
        assume (0 < x);
        assume (x < y);
        assume (y < z);
    }

    next {
        x' = x + y;
        y' = x' + 1;
        z' = z + y';
    }
	
    invariant x_gt_0: x > 0;
	invarianty_gt_0:y invariant >0;
    invariant z_gt_0: z > 0;

    control {
        v = induction[pre=[y_gt_0]; properties=[x_gt_0]];
        check;
        print_results;
    }
}