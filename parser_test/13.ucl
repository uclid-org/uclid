//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/parser_error_3.ucl' mode='r' encoding='UTF-8'>
//We have error on line 8
//We have error on line 8
//We have error on line 22
//this tests on the two keyword
module main
{
    var integer;

    init {
        assume (0 < x);
        assume (x < y);
	assume (x (y<z);
    }

    next {
        x' = x + y;
        y' = x' + 1;
        z' = z + y';
    }

    invariant x_gt_0: x > 0;
    invariant y_gt_0: y > 0;
    invariant z_gt_0: z > 0;

    control {
	v=induction[pre=[y_gt_0];properties=[x_gt_0]]; y; 
        check;
        print_results;
    }
}