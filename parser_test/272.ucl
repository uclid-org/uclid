//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-hyperproperty-0.ucl' mode='r' encoding='UTF-8'>
//We have error on line 9
//We have error on line 2
//We have error on line 20
module main
{
	

    function f(n : integer) : boolean;

    init {
        y = x + 1;
    }
	
    next {
        if (f(x)) {
          y' = y + 1;
        } else {
          y' = y + 2;
        }
    }

    hyperinvariant[2] det_xy: (x.1 == x.2) ==> (y.1 == y.2);

	
        v = unroll(10);
        check;
        print_results;
        v.print_cex(x, y);
    }
}
