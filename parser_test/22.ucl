//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-procedure-checker-1.ucl' mode='r' encoding='UTF-8'>
//We have error on line 17
//We have error on line 22
//We have error on line 19
module main
{
  input n  : integer;
  var nums : [integer]integer;
  var x : integer; 

  procedure sum(n : integer) returns (numbers : [integer]integer)
    modifies z;
    requires (n > 0);
    requires (n < 5);
    ensures (forall (i : integer) :: (i > 0 && i < 5) ==> numbers[i] == i);
  {
    for i in range(1, 5) {
      if (i < n) {
        numbers[i] = i;
      }
    }
	
  }
	
  next {
    call (nums') = sum(n);
	

  control {
    print_module;
  }
}
