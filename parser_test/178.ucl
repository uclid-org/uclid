//we gather 1 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-synthesis-5.ucl' mode='r' encoding='UTF-8'>
//We have error on line 31
//We have error on line 23
module main
{
  synthesis function h(x : integer, y : integer, z : integer) : boolean;
  var x, y, z : integer;

  init {
    x = 0;
  }

  next {
    if (x < 2) {
      x' = x + 1;
      if (*) {
        y' = z';
        havoc z;
        assume (z' <= y);
      } else {
        y' = y;
        havoc z;
        assume (z' > y);
      }
    }
  }
	
  property post_f : !(x >= 2 && (z < y));
  invariant hole: h(x,y,z);

  control {
    v = unroll(10);
    check;
    print_results;
	
}
