//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-mod-set-analysis-4.ucl' mode='r' encoding='UTF-8'>
//We have error on line 0
//We have error on line 19
//We have error on line 2
	

	
    var y: integer;

    procedure [noinline] bar()
        ensures x == old(x) + 1;
    {
        x = x + 1;
    }

    // Check that calling bar does not change y
    procedure foo()
        ensures y == old(y);
        ensures x == old(x) + 1;
    {
        call bar();
    }

	
        v = verify(bar);
        v = verify(foo);
        check;
        print_results;
    }
}
