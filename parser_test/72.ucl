//we gather 3 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-exprs-1.ucl' mode='r' encoding='UTF-8'>
//We have error on line 5
//We have error on line 30
//We have error on line 25
//We have error on line 9
module main
{
  var sum, fake, i : integer;
  
  init {
	
  }
  
  procedure next_state()
	
  {
    havoc sum;
    havoc fake;
    assume (fake == (sum+sum+1));
    assert (fake == (sum+1+sum));
    havoc fake;
    assume (fake == (sum + sum));
    assert false;
    i = i + 1;
  }
  next {
    assert ((i == 0) || (fake == 2*sum));
    call next_state();
  }
  
	
    unroll (2);
    check;
    print_results;
  }
	
