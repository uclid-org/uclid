//we gather 2 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-synthesis-6.ucl' mode='r' encoding='UTF-8'>
//We have error on line 5
//We have error on line 19
//We have error on line 17
module main
{
  synthesis function h(x : integer, y : integer) : boolean;
  var y, z, p : integer;

	init{ assume 
    assume (z >= y);
  }

  next {
    havoc p;
    if (*) {
      y' = z';
      z' = p;
      assume (z' <= y);
    } else {
      y' = y;
	z'=p; 
      assume (z' > y);
	} 
  }
  
  invariant not_p : z >= y;
  invariant hole : h(y, p);
  
  
  control {
    // print_module;
    v = unroll(5);
    check;
    print_results;
  }
}
