//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-assert-1.ucl' mode='r' encoding='UTF-8'>
//We have error on line 21
//We have error on line 26
//We have error on line 23
module main
{
    type color_t = enum { red, blue };

    var red_cnt : integer;
    var color : color_t;

    procedure next_color()
      modifies color;
    {
      color = blue;
      if (red_cnt > 0) {
        color = red;
      }
      if(color == red) {
        assert (red_cnt > 0);
      }
    }

	init { }
	
	
      call next_color();
	

    control {
	
      x = unroll(1);
      check;
      print_results;
    }
}
