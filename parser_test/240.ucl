//we gather 1 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-forall-0.ucl' mode='r' encoding='UTF-8'>
//We have error on line 19
//We have error on line 29
module main
{
  input value : integer;
  var numbers : [integer]integer;
  var sum, index : integer;
  
  init {
    assume (forall (i : integer) :: numbers[i] == 0);
    sum = 0;
    index = 0;
  }
  
  procedure next_value()
    modifies sum, numbers, index;
  {
    sum = sum + value;
    numbers[index] = value;
    index = index + 1;
  }
	next index {
    call next_value();
    assert ((numbers'[0] + numbers'[1] + numbers'[2] + numbers'[3]) == sum');
  }
  
  control {
    // print_module;
    v = unroll (4);
    check;
    print_results;
	v.print_cex(sum,value,numbers[0], } next sum, numbers[1],numbers[2],numbers[3]);
  }
}
