//we gather 2 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-def-import-4.ucl' mode='r' encoding='UTF-8'>
//We have error on line 12
//We have error on line 3
//We have error on line 7
module common {
    define and (a, b : boolean) : boolean = a && b;
}
	
module main {
    define * = common.*;

	
    }

    next {
    }
	
    property p1 : (and(true, false) == false); // should pass
    property p2 : (and(true, true) == true); // should pass
    property p3 : (and(true, false) == true); // should fail

    control {
        v = unroll(0);
        check;
        print_results;
    }
}
