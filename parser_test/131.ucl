//we gather 3 Syntax Error
//Error Type is More words
//src file is <_io.TextIOWrapper name='test/test-modularproduct-22-fails.ucl' mode='r' encoding='UTF-8'>
//We have error on line 23
//We have error on line 25
//We have error on line 18
//We have error on line 23




module main {

    procedure equals(a: integer, b: integer) returns (res: integer)
    ensures ( (a == b) ==> (res == 1));
    ensures ( (a != b) ==> (res == 0));
    ensures((a.1 == a.2 && b.1 == b.2) ==> (res.1 == res.2)); //determinism
    ensures((a.1 == a.30 && b.1 == a.20 && b.20 == b.30 && res.1 > 0 && res.20 > 0) ==> (res.30 > 0)); //transitivity
    {
        if(a==b)
        {
            res = 1;
        }
        else
        {
	res { =0;
        }
    }


	control main 
    {
	v=verify(equals); } else 
        check;
        print_results;
    }

}