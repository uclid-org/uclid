//we gather 3 Syntax Error
//Error_Type is less words
//src file is <_io.TextIOWrapper name='test/test-int-fib.ucl' mode='r' encoding='UTF-8'>
//We have error on line 5
//We have error on line 11
//We have error on line 13
//We have error on line 26
module main {
  var a, b : integer;
  procedure fib(x: integer, y: integer) returns (z:integer, w:integer) { 
    z, w = y, x + y;
  }
	
  init {
    a = 0b0; // binary literal
    b = 0x1; // hexadecimal literal
  }

	
    call (a', b') = fib(a, b);
	
    assert (a' <  b');
    // These weird assertions are to check the +/- arithmetic operators.
    assert (a' + b' <= b' + b');
    assert (a' - a' <= b' - a');
  }

  control {
  	v = unroll (3);
  	check;
  	print_results;
    v.print_cex(a, b);
  }
	
