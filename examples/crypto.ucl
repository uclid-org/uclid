module main
{
  var pt, ct, dt : integer;
  const sk       : integer;
  input ik       : integer;

  init {
    ct = enc(sk, pt);
  }

  next {
    dt' = dec(ik, ct);
    assert (dt' == pt) ==> (ik == sk);
  }

  control {
    v = unroll(5);
    check;
    print_results;
  }
}
