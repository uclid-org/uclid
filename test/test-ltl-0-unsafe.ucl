module main
{
  input a, b : int;
  var inited : bool;
  output sum : int;
  
  init {
    inited = false;
  }
  next {
	inited = true;    
    sum = a + b;
  }
  
  property p1[LTL] : G(inited ==> (sum == a + b + 1));
  
  control {
    vobj = bmc (3);
    check;
    // print_results;
  }
}