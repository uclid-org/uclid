module main {

	var a: integer;
	var b: integer;

	init {
		a = 1;
		b = 2;
		assume((a == 1) ==> (b == 2));
		assert(a == b);
	}

	invariant some:
		forall(x: integer, y: integer) :: forall(x: integer) :: (x == y) || (x != y);

}
