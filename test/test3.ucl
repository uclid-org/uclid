module main {
	var a: integer;
	var b: integer;
	var c: integer;
	var d: integer;

	init {
		a = 1;
		b = 2;
		//a = 10;
		c = 3;
		//d = 4;
		//assume(a < 0);
	}

	next {
		a' = a + 1;
		b' = a' + 2;
		c' = b + a';
		d' = c' + c;
		assume(d' > 0);		
	}
	invariant some: a > 0; 

	control {
		v = unroll(1);
		check;
		print_results;
	}
}
