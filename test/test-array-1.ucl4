module main
{
  var sum : int;
  var fake : int;
  
  init {
  }
  
  next {
    assume (sum == 1);
    assume (fake == 10);
    havoc fake;
    assume (fake == (sum + sum));
    assert (fake == 2*sum);
    assert false;
  }
  
  control {
    initialize;
    simulate (5);
    decide;
  }
}
