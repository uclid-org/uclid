module main
{
  type node_t = record { user : integer };
  input user : integer;

  init { }

  procedure create_node(user : integer)
    returns (node: node_t)
  {
      node.user = user;
  }

  next {
  }
}
