module main
{
    var x, y : integer;

    init {
        y = x + 1;
    }

    next {
        y' = y + 1;
//        assert (y' > 0);
//        assert (y.1 == y.2);
    }

    hyperinvariant[3] det_xy: (x.1 == x.3) ==> (y.1 == y.3);

    control {
        unroll(5);
	check;
	print_results;
    }
}
