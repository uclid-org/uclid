/*
Test-conctete-6:
1. test of assume
2. test of Fuzzing
*/
module main
{
    var a : integer;
    var b : integer;
    init {
        assume(a==3);
    }
    next {
        a' = a + 1;
        assert(a>=3);
    }

    control {
        v = concrete(5,"Default");
        check;
        print_results;
    }
}
