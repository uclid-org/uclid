module main
{
    var a : integer;
    var b : integer;
    init {
        a = -1;
    }

    next 
    {
        case
        (a <= 0) : 
        {a' = 6;}
        
        (a < 0) :
        {a' = 5;}
        esac
    }
    property jump_a: a!=3;

    control {
        v = concrete(5);
        check;
        print_results;
        //v.print_cex(n);
    }
}
