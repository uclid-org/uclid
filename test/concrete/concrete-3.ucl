/*
  concrete-3.ucl
  1. test of branch
  2. test of assert(false)
*/
module main
{
  var flag : boolean;
  
  init { flag = false; }
  next {
    if (flag) {
      skip;
    } else {
      assert (false);
    }
  }
  
  control {
    v = concrete(5);
    check;
    print_results;
  }
}
