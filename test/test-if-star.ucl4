module main {
  var x : int;
  var y : int;
  var n : int;
  
  init {
    x := 1;
    y := 1;
    n := 2;
  }
  
  next {
    if (*) {
      x := x + 1;
    } else {
      y := y + 1;
    }
    
    n := n + 1;
  }
  
  property eq_nxy : (n == (x + y));
  /*
  property le_xn  : (x <= n);
  property le_yn  : (y <= n);
  property lt_xn  : (n > 0) ==> (x < n);
  */

  control {
    initialize;
    simulate(5);
    decide;
    print_results;
  }
}