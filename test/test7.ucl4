module testModule {
       
  var v1: int;
  var arr: [int] int;
  type t1 = int;

  procedure p1(a1: int, a2: int) {
    v1 := v1 + a1 + arr[a2];
  }
  procedure p2(a3: int) {
    arr[a3] := arr[a3] + 4;
  }

  init {
    havoc v1;
    havoc arr;
  }
  next {
    call () := p1(1,2);
    call () := p2(7);
    assert v1 < 5;
  }
}
