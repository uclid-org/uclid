module main {
  function f(g : int) : int;
  
  var x : int;
  var y : int;
  
  init {
    x := 0;
    y := 0;
  }
  
  next {
    x := f(x);
    y := f(y);
    
    assert (x == y);
  }
  
  control {
    initialize;
    simulate (5);
    decide;
  }
}