// Test that an if-else statement with non-boolean condition results in a compiler error.

module main {
  var a : int;
  var b : int;

  init {
    a := 0;
    b := 1;
    if(a) {
      b := 2;
    }
  }
  next {
    a := a + 1;
  }

  control {
    initialize;
  	simulate (5);
  }
}