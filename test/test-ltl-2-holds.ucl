module main {
  var state : bool;
  
  init {
    state = false;
  }
  
  next {
    if (*) {
	  state = !state;
	}
  }
  
  property ss[LTL] : G(F(state));
  
  control {
    print_module;
    bmc(5);
    check;
    print_results;
  } 
}
