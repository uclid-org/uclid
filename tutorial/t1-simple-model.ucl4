module main {
  var a, b : int;

  init {
    a = 0b0; // binary literal
    b = 0x1; // hexadecimal literal
  }
  next {
    a, b = b, a + b;
  }

  property a_le_b: a <= b;
  
  control {
    unroll (3);
    decide;
    print_results;
  }
}
